HOST=0.0.0.0
PORT=4173
VPBUDDY_API_BASE_URL=http://47.100.182.3:28765

# Optional: expose a different browser-visible API origin instead of the built-in
# same-origin proxy. The value must be an absolute HTTP(S) URL.
# VPBUDDY_PUBLIC_API_BASE_URL=https://api.example.com
